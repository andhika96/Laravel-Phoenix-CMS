# Image Box Title and Text Effects Design QA

- source visual truth path: `output/design-qa/image-box-text-effects-before.png`
- implementation screenshot path: unavailable
- viewport: source crop 362 x 651 px; implementation viewport unavailable
- state: Image Box > Style > Content, Title Text Stroke and Text Shadow controls
- full-view comparison evidence: blocked because the in-app browser runtime could not start under the Windows sandbox
- focused region comparison evidence: not available for the same blocker

## Findings

- [P1] Rendered implementation cannot yet be visually compared
  - Location: Image Box settings sidebar and canvas.
  - Evidence: the source screenshot is available, but no post-patch browser screenshot could be captured because the browser runtime exits during sandbox initialization.
  - Impact: typography hierarchy, popover overlay geometry, clipping, and interaction state cannot be honestly approved from source code alone.
  - Fix: run the same-state browser check with an approved fallback browser runner, capture the implementation, place both images in one comparison input, and patch any visible P0-P2 differences.

## Open Questions

- Permission is needed before using Playwright directly as the fallback browser runner.

## Implementation Checklist

- Capture the updated Image Box Style > Content panel at the same viewport/state.
- Verify H1 computes to 40px and H6 computes to 16px in automatic mode.
- Edit Typography Size and verify the custom value overrides tag scale.
- Verify only one Text Stroke/Text Shadow popover is open at once.
- Check console errors and compare source versus implementation side by side.

## Patches Made Since Previous QA Pass

- Added automatic H1-H6 title scale with custom Typography override.
- Refactored Text Stroke and Text Shadow into compact icon-triggered popovers.
- Added one-active-popover coordination and icon-only reset actions.
- Removed native number spinners from text-effect numeric fields.

## Follow-up Polish

- Deferred until a rendered implementation screenshot is available.

final result: blocked
