# Compiled Native mapper expansion QA

Date: 2026-08-30
Project: `D:\Laragon\www\laravel-13-phoenix`
Plan: `project-artifacts/plans/2026-08-30-pagebuilder-v24-compiled-native-mapper-expansion-plan.md`

## Outcome

The v2.4 Compiled Native pipeline now retains measured DOM evidence and adds a relationship layer for normal flow, positioned layers, containing blocks, overlap pairs, stacking order, pseudo-elements, and responsive property deltas. A valid Stack/Grid/Flex root is no longer rejected solely because a reconstructable absolute/fixed child exists.

Positioned children are mapped to existing Advanced settings (`position`, horizontal/vertical orientation, responsive X/Y offsets, z-index, and custom width). Grid desktop → one-column mobile transitions use the existing responsive Grid settings; Flex ↔ Grid transitions remain explicit manual decisions because the existing node type cannot switch safely at runtime.

Widget candidates remain evidence-based and editable. Structural wrappers are kept as Container/Grid, while Icon Box/Image Box/composite candidates use nested members without duplicate mapping rows. Registered candidates with score `>= 0.60` are preselected.

## Asset and residual-style behavior

Local package assets receive stable `mappingId`, safe `sourcePath`, availability, and nullable `targetUrl`; filesystem paths are removed before the browser response. The target preview exposes explicit Local asset mapping inputs and does not upload or persist files during analysis. Image/Image Box Apply remains blocked until a safe persistent URL is supplied.

Native-safe style is applied to Canvas settings. Unsupported-but-safe residuals produce scoped CSS patches as review notes. Custom JavaScript is not used for layout or CSS fallback. Transform/overlap ambiguity, missing layout evidence, unsupported mode transitions, and unresolved assets remain blocking with source/viewport remediation.

## Real-source replay

Source: `E:\Apps\Laragon\www\ceo-masters\index.html`

- Framework: `tailwind_static`
- Measured nodes: `351`
- `home`: Stack on desktop/tablet/mobile, `layout-ready`, 17 normalized mapping nodes, 24 responsive-delta nodes, one non-blocking box-sizing review note.
- `register`: Stack on desktop/tablet/mobile, `layout-ready`, 16 normalized mapping nodes, 11 responsive-delta nodes, one non-blocking box-sizing review note.
- `register` positioned-layer blockers: none in the current replay. The earlier `register desktop/tablet/mobile` message came from the previous classifier rejection path; the updated classifier now preserves a valid root layout.
- No server filesystem path appeared in the browser-facing asset manifest.

Replay output: `MAPPER_EXPANSION_REPLAY.json` in this QA directory.

## Verification

- Automatic Compiled Native PHPUnit suite: **80 passed, 384 assertions**.
- Full v2.4 Node suite: **423 passed, 0 failed**.
- Focused mapping Canvas-flow contract: **9 passed, 0 failed**.
- Measurement contract including pseudo-elements: **4 passed, 0 failed**.
- `node --check public/js/pagebuilder_elementor_v24/app.js`: passed.
- PHP lint for Compiled Native sources/controller/request/route: passed in the final lint gate.
- `git diff --check`: passed.
- Graphify refreshed after source changes: **21,142 nodes / 39,014 edges**.
- Graphify query confirmed the scanner → relationship graph → classifier → mapper → target validation path.
- v2.3 diff audit: `V23_DIFF=NONE`.

## Boundaries

- Live authenticated import click-through at all three viewports was not executed in this QA session; fixture measurement and static/runtime contracts cover the behavior, but this is not a substitute for live browser evidence.
- No Save or Apply action was executed.
- Local asset mapping currently accepts a persistent URL explicitly; a dedicated File Manager picker/upload flow is not part of this slice.
- Complex transforms, fixed layers tied to viewport behavior, pseudo-elements that need to become editable widgets, fonts, and JavaScript interactions may still require explicit review.
- Pixel parity is not claimed from candidate scores or structural validation alone.
