# Product Lead Form v2.4 — Browser QA

Tanggal: 2026-08-23 16:57 WIB  
Surface: Chrome authenticated session, temporary draft page `31` (`qa-product-lead-form-browser-20260823-1529`)  
Batasan: read-only QA; tidak menekan Save, Apply Dataset, Reset, atau submit form.

## Hasil

- Dataset shared `New Dataset` termuat setelah widget dipilih dan hydration selesai; 3 node Model tampil.
- Cards memakai semantic `radiogroup` dengan 3 `radio`, checked state, keyboard-native input, dan thumbnail `alt`.
- Hover preview: `.is-preview-hover` aktif; media border `solid`, sekitar `3px`, warna `rgb(239, 68, 68)`, background `rgb(254, 242, 242)`. Mengubah Hover Border ke `#00aa00` mengubah canvas ke `rgb(0, 170, 0)` dan dipulihkan tanpa Save.
- Selected preview: `.is-preview-selected` aktif; media border sekitar `4px`, warna `rgb(37, 99, 235)`, background `rgb(239, 246, 255)`. Checklist terukur `18px`, glyph `8px`, offset `14px` pada baseline.
- Label Position: `Above image` menempatkan label sebelum thumbnail dengan gap sekitar `12px`; `Below image` sesudah thumbnail dengan gap sekitar `12px`; `Inside image` memakai `position:absolute` dan overlap media.
- Label Gap: angka `20` menghasilkan `row-gap: 20px` dan jarak media-label sekitar `20px`.
- Image Shape: `Rounded` menghasilkan radius `8px`; `Circle` menghasilkan radius `50%`.
- Image Height: unit `%` tersedia dan nilai `50%` menghasilkan inline style `height: 50%`; dipulihkan ke `120px`. Image Width slider memakai max `600`; Card Width memakai max `1200` saat unit `px` (unit `%` dibatasi `100` secara benar).
- Presentation: `Cards` menampilkan radiogroup; `Select` menghilangkan radiogroup dan menampilkan native `<select>` dengan 3 option; dikembalikan ke Cards.
- Form binding: input `name` dapat diisi `QA Browser`; field Type `Text → Textarea → Text` mengubah markup canvas dan kembali normal.
- Responsive editor: device `Mobile` menghasilkan media position `top`, satu kolom body, dan 2 kolom card; `Mobile Media Position = bottom` menempatkan form lebih dahulu lalu media; dikembalikan ke Desktop/top.
- Custom Messages: Preview `Toast` membuat layer `display-toast` absolut pada `.webpage-frame` dengan `top:16px; right:16px`; Preview `Modal` membuat overlay sekitar ukuran penuh frame (`1178.5×887.7` pada frame `1180×889`) dengan dialog `alertdialog` terpusat. Tidak ada layer yang dibatasi hanya pada form.
- URL editor tetap `/pagebuilder-elementor/v2.4/edit/31`; preview lokal tidak memutasi URL editor.
- Console errors/warnings setelah seluruh interaksi: `[]`.

## Verifikasi automated setelah QA

- `node --test tests/pagebuilder-v24-product-lead-form.test.mjs`: 10 passed.
- `node --test tests/pagebuilder-v24-*.test.mjs`: 388 passed, 0 failed.
- `php artisan test --filter=PageBuilderElementorV24`: 154 passed, 10,310 assertions.
- `npm.cmd run build`: sukses, 58 modules transformed.
- `php -l` frontend Blade: tidak ada syntax error.

## Catatan

- Suite `node --test tests/*.test.mjs` tidak dipakai sebagai acceptance karena ikut menjalankan test tema Prism di luar scope dan gagal pada ekspektasi header lama; suite v2.4 scoped tetap seluruhnya hijau.
- Query deep-link, ownership/tamper, email/webhook, dan submit nyata tetap diverifikasi lewat unit/feature/fake tests; browser QA sengaja tidak mengirim data.
